class Operaciones {
    constructor () {
        this.valorTemporal;
    }

    sumar(valor1, valor2) {
        return valor1 + valor2
    }

    restar(valor1, valor2) {
        return valor1 - valor2
    }

    multiplicar(valor1, valor2) {
        return valor1 * valor2
    }

    divir(valor1, valor2) {
        return valor1 / valor2
    }

    potenciaPor2(valor) {
        return valor * valor;
    }
}

const mate = new Operaciones();
let resultado = mate.
console.log(resultado);
